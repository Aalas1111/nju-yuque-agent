# 运行时工作区（含 session 留痕、产出、快照）——不入库
/workspace/

# 凭证：绝不允许提交
.env
*.key
auth.json
# QQBot 凭证（扫码登录的落点；也认 --credentials 指到别处）
qqbot.json

# Python
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/

# 本地临时
*.log
